# Normal-mode input timing — September 20, 2026

## Evidence and scope

Sai requested normal-mode timing repairs after suspecting BLE latency cuts the gesture snippet. The existing Simple motion work remains opt-in and separate. This pass changes software recognition/pairing, without firmware or transport protocol changes.

Both supplied browser traces have complete streaming packets: 550/550 samples in 08:22 and 6437/6437 streaming/validation samples in 08:46 (14 earlier notifications occurred during handshake). Neither has sequence loss, rejected streaming samples, device drops or discontinuity flags. Acquisition intervals remain 18–23 ms despite notification batching up to 90.5 ms. The maximum accepted age bound is 132 ms. The data does not support blaming signal loss or increasing stale-packet allowances.

Two actual timing defects were identified:

- Gesture onset retained only the previous sample (~20 ms). A subthreshold upward launch can be lost before a stronger brake crosses onset. Retaining 120 ms of feature context recovers launch/brake order. Onset timing, duration floors and personal DTW exclude this added context, preventing quiet padding from creating a gesture.
- Every five seconds, a clock probe replaces the capture-to-browser offset. The longer trace has 26 changes of −10.25 to +23.75 ms; regular acquisition can appear as isolated 10.1–43.75 ms intervals. A concrete 63 ms candidate appeared to last 55.3 ms. Gesture shape and jerk now use acquisition-clock deltas; each active movement keeps one browser-clock anchor for speech alignment. Correcting duration alone did not recover that particular recorded candidate, which also failed other gates.

Clock discontinuities between movements still reset rest tracking. Removing that existing reset exposed a false jab in Protego trial 8: a small resting-baseline difference moved a return just below the old 18° lowering threshold. The narrower repair preserves recorded negatives while preventing clock corrections from shortening or breaking an active movement. Actual capture gaps, age checks, flags and generation resets still invalidate discontinuous evidence.

## Pairing changes

Normal mode retains up to eight recent typed gestures, selects a temporally compatible matching class using captured intervals, and consumes the attempt once. An unrelated or wrong candidate no longer immediately discards a good word. Jab/raise requirements remain.

The initial v4 pass increased the normal-mode gap from 350 to 900 ms. The subsequent v5 pass allows **two seconds in either direction** and a **six-second total span**, with a seven-second maximum pending lifetime. These are capture-time bounds, not a delay before casting: a complete matching pair submits immediately. A mismatch fizzle waits for the opportunity to close. The one-second speech-result deadline, one local inference job, sample-age/gap guards, generation resets and duplicate consumption remain. Simple motion keeps its prior 350 ms / two-second limits.

A newly confirmed word can replace an older unmatched word. A newer sound holds overlapping movement until it resolves. Only a validated, timely, silence-ended `no-speech` or `not-an-incantation` result restores pairing with the retained word. Ambiguous cancellation, low confidence, timeout and cutoff clear the attempt rather than assign a second command's movement to the first word. Only one confirmed word and one active recognition are retained, with bounded motion history. The speech client additionally remembers one completed natural-inference interval so a delayed onset captured while inference was busy remains suppressed after the result arrives.

## Initial v4 verification

- `npm run typecheck` and all **329 web unit tests** pass across 23 files. The affected suites include 45 motion, 34 fusion, 33 speech client and 19 controller tests. No lint command is configured in the web package.
- Motion regressions cover subthreshold upward launch versus reverse lowering, stale/broken pre-roll, measured −10.25/+23.75 ms and synthetic −30 ms mid-burst clock corrections, 32-bit clock wrap, the next movement's fresh anchor and real acquisition discontinuities.
- Six private capture replays retain the same accepted spell decisions: all 20 labelled raises, nine of ten labelled jabs and zero activations within the five completed Still Move windows. Whole-session totals remain 13/10/12/2/3/19 (they include return/rest movement; the two Still counts use stateful versus fresh-profile startup). Thirteen accepted endpoints receive acquisition-clock timing corrections. These captures informed the repair and are not an independent accuracy test.
- A separate read-only review found two regressions before completion: suppressed noise could retrigger after forced cutoff, and unrestricted rest-clock changes exposed an extra return jab. Both have been fixed; the final review found no remaining actionable defect in this scope.
- `npm run test:e2e` passed 31 of 32 scenarios, including normal-mode batched sensor delivery, a recorded badge raise, five-spell solo and multiplayer play, wrong-gesture fizzles/recovery, cooldowns, victory/rematch/reconnect, tutorial, phone input and browser audio capture. The new timing scenario delivers raw samples in 100 ms batches, then supplies recognized speech 650 ms after the typed movement; Protego and Stupefy each receive one referee acknowledgement with Dev mode off and no rejected samples. Only speech recognition is scripted; raw packets traverse the actual decoder, recognizer and fusion.
- The remaining Simple motion scenario exposed a test timestamp defect: mapped motion time could lead `performance.now()` by ~0.95 ms, so an instantaneous scripted speech final incorrectly preceded its own capture end. Its helper now clamps the final timestamp to the capture end, matching the ordinary cast helper. The corrected `npm run test:e2e -- --grep 'dev-only simple motion'` rerun passed, completing all 32 scenarios across the full run and targeted rerun. Production timing guards were not relaxed.
- Normal-mode battle/HUD screenshots were inspected (`/tmp/wandduel-normal-timing.png`, `/tmp/wandduel-solo-battle.png`); player health remains above the player and rival health beneath the rival, with ordinary JAB/RAISE spell guidance intact.
- The owned local stack was rebuilt and restarted with `python3 tools/run_game.py --referee https://wandduel-referee.onrender.com`. The production build passed, frontend returned HTTP 200, the served bundle contained the v4 normal profile and delayed-onset fix, local speech reported ready/warm/worker available, and the referee reported multiplayer ready. The existing browser tab was reloaded and inspected at the homepage with Dev mode off. Startup log: `/tmp/wandduel-normal-timing-stack.log`.
- Origin/main was fetched and still matches `c94e64b`; no teammate changes needed reconciliation. This was the local, uncommitted verification checkpoint before publication.

The prior stale guard-direction veto is a distinct classification issue. Increasing a pairing window cannot manufacture a gesture that was never accepted. Its two-second timeout proposal still needs long-held lowering negatives; this timing pass does not claim it is solved.

## V5 speech-first and multiplayer follow-up

The prior 900 ms window left unequal time to react to feedback: gesture evidence in the supplied traces arrived about 192 ms after movement (184–209 ms for raises), while accepted speech arrived 509–542 ms after voice end. Both still used captured intervals correctly, but speech-first players had less time remaining. The new two-second bound applies symmetrically and does not add callback latency to captured movement duration.

A movement that finished before a newer sound started could be held by that sound merely because classification arrived later. Capture-time ownership now lets that completed movement pair with the earlier word while retaining the newer utterance for its own next movement. A movement overlapping a newer utterance still waits for its result; a newly confirmed spell replaces an unmatched older word. Final review rejected unconditional fallback after cancellation: forced-cutoff continuation, timeout, interruption, low-confidence and unknown results clear both words and buffered movement. Only explicit, valid, timely, silence-ended non-spell results preserve the earlier word. This prevents an interrupted second command from firing the first spell with the second command's movement.

An audio-clock lifecycle reproduction found that a visible AudioContext suspension could pause its frame clock while browser time continued, leaving future contiguous frames incorrectly timestamped. The capture adapter now retires a stopped/interrupted audio context and queued callbacks; a fresh microphone session gets a fresh clock origin. It does not restamp delayed audio or claim that this interruption occurred in Sai's last test.

Accepted-cast telemetry now includes both capture intervals, speech final time, order and measured gap, under normal profile `quick-play-jab-raise-v5`. This makes later separation failures measurable without deriving the interval from UI feedback.

Multiplayer presentation also had a separate delay failure: authoritative HP could update while the own-cast animation event was discarded by the 300 ms event-age guard. The controller now correlates a pending cast with its referee acknowledgement for up to two seconds and presents acceptance exactly once. Bounded pending/action histories, room/round/input generations and lifecycle checks retire stale acknowledgements; no optimistic damage or retries were added.

Verification:

- Web typecheck and all **364 unit tests across 23 files** pass, including 45 speech, 39 fusion and 37 controller tests. Eight speech/fusion integration cases cover confirmed noise versus low confidence, unknown reason, forced cutoff, interruption and timeout. Independent final source review found no remaining actionable defect in this scope.
- The full existing browser suite passed **33/33 scenarios** before the final conservative discard refinement. All four affected post-review scenarios also passed: both timing tests, wrong-gesture fizzle/recovery and actual browser audio capture. Hosted checks and the final stack restart follow below.
- The existing browser harness drives synthetic PCM through the actual AudioWorklet, endpoint and SpeechClient; only ASR decoding is mocked. Raw BLE samples arrive in 100 ms batches. Solo tests cover Protego and Stupefy with overlapping capture and measured 1.2–1.6 second gaps in both orders, exactly one accepted cast each, and no rejected samples.
- The two-player timing test uses independent browser contexts and audio pipelines, the real local referee, and an extra 175 ms outbound cast delay. Both players cast in all three orders; each receives one accepted acknowledgement and both views agree on HP (80, 60, then 40). Controller regressions separately cover 500 ms acknowledgement delay, duplicates, rejection and stale lifecycle/room/round cases. These tests qualify the deployed referee protocol, not current internet latency or fresh human speech accuracy.
- The final stable local stack was rebuilt and restarted with `python3 tools/run_game.py --referee https://wandduel-referee.onrender.com`. Frontend HTTP 200, served v5 profile and rival-ready gate, speech ready/warm/worker available, and hosted multiplayer readiness were verified. The existing browser was confirmed idle, reloaded and visually inspected with Dev mode off. Startup log: `/tmp/wandduel-aligned-input-stack.log`.
- At the pre-release verification checkpoint, main and origin/main both matched `c94e64b`. The results above were recorded before the subsequent release commit.

A direct run of the same two-player test against Render found a lobby race that local near-zero latency hid. The first player could Ready before the second player's session request completed; `add_session()` then reset lobby readiness. The first Ready acknowledgement was accepted, yet the final state had P1 not ready and P2 ready. This reproduced on two hosted runs; two other runs reached battle and completed all six timed casts. The browser/controller now wait for the opposite slot to be connected before allowing multiplayer Ready; solo/tutorial remain independent. The existing test adds a delayed join request and verifies the waiting state. Both consecutive post-fix hosted runs passed (25.0 s and 24.5 s). Across their 12 accepted casts, captured sequential gaps were 1412.8–1438.2 ms in either direction, scripted outbound delay was 176.0–183.4 ms, and actual sent-to-ack delay was 44.8–76.4 ms. All inputs remained healthy with zero rejected samples, and both player snapshots reached 40 HP. Evidence is retained under `/tmp/wandduel-render-fixed-results/`, including per-cast capture intervals and final shared state. The Render dashboard showed the live service at `c94e64b`, matching the unchanged referee source; no redeployment was needed to exercise the new browser code against it. All five affected local browser scenarios then passed: full solo/rematch/reconnect, tutorial, wand-first multiplayer entry, the two-player timing test with delayed join, and five-spell multiplayer/victory/rematch/recovery. The modified E2E sources also pass TypeScript checking. The narrow-screen lobby and normal battle screenshot were visually inspected.

Render hosts only `apps/host`; each laptop runs its own web bundle and speech helper. Capture-time fusion happens before the unchanged cast message goes to Render. These input changes require publication, a pull, a full launcher rebuild/restart and a browser reload **on every laptop**. Redeploying Render alone cannot deliver them; there is no referee, firmware, phone-worker or wire-schema migration in this pass. Windows and fresh physical input remain separate qualification steps.

## Physical retest

1. Reload the rebuilt local game and keep Dev mode off. Pair the badge, enable the laptop microphone and enter a solo duel. No firmware flash is required.
2. Try five raises with Protego and five jabs with Stupefy, respecting cooldowns. Try saying the name during the movement, then a little before or after it (within roughly half a second). Return to rest between attempts.
3. Speak without moving, move without speaking, and say Protego with a jab. None should cast. A confirmed word with incompatible movement may wait up to seven seconds before the fizzle, allowing a delayed matching candidate to arrive.
4. On two updated laptops, each pair a wand and enable the microphone. Create/join one room; Ready should remain unavailable until the rival connects. Ready both, then trade Stupefy and Protego attempts in overlap and both orders. Confirm damage and shields agree on both screens. Test once after a microphone disable/re-enable and badge reconnect.
5. For a failed attempt, repeat with Dev mode on and **Simple motion off**, then export telemetry. This uses the same typed recognizer while recording raw samples, clock timing, candidate decisions, speech and cast acknowledgements. Do not click spell cards during capture.

Replayed sensors and scripted speech qualify software timing and pairing, not fresh physical recognition or microphone accuracy. Long-held lowering and ordinary play still need the connected badge in the user's hand.
