# Gesture tolerance, failed-cast feedback and labelled trials — September 20, 2026

## Request and observed evidence

Sai requested committing/pushing main, easier gestures (especially badge raises), visible failed-cast feedback, and a ten-trial recording method. Main was already synchronized at `8e5ee93`; this iteration keeps main and the existing input protocol.

The supplied `wandduel-telemetry-2026-09-20T06-45-51.558Z.json` reports **PHONE**, tutorial mode and developer mode. Its retained window is approximately 6.8 seconds: 284 raw/decoded motion records, one gesture accepted as Stupefy, four speech discards and no transcription result, cast attempt or referee acknowledgement. Observed sample cadence is 41.8 Hz, maximum sample interval 34 ms and age-upper-bound p95 65.1 ms, with no sample marked as breaking a gesture. It has no intended-action labels or retained lesson-state event. It cannot establish a badge failure or prove which spell Sai said. It demonstrates that the existing diagnostic export needed more context after clearing the log.

The accepted movement committed on the early impulse path at 432 ms despite a 144-degree apparent ending-vector rotation. Motion continued afterward. Replaying the same motion after this change delays classification until settling (1533 ms) and still classifies Stupefy; the trace is **not** relabelled as a successful raise. The sanitized motion-only fixture preserves original sample timings, age bounds, flags and axes; it contains no transcript, credentials or personal device identity.

## Implementation and reuse

- Reused `MotionRecognizer`, default profile, existing raw replay fixtures and transport validation. Found a specific issue: the direction-free generic jab matched a brisk raise before the hold path could classify it. Quick play now defers plausible tilted raises, lets a valid held raise win over generic impulse, lowers the default jab floor from 525 to 400 mg and guard tilt from 22 to 18 degrees. Learned calibration thresholds and packet timing/age/gap validity remain unchanged. Exported profile is `quick-play-jab-raise-v2`.
- Reused `CastFusion` to report bounded rejections only after a confirmed incantation: wrong gesture, missing clear movement, multiple candidates or timing mismatch. Reset, stale input, cancelled ASR and silent movement do not produce failed spells. Controller feedback is local; no server cast, health effect or cooldown is fabricated. A successful cast, changed round/lesson, disconnect, hidden page or expiry clears it.
- Added original SVG/CSS feedback near the local wand: fractured Protego shield, collapsing Episkey wisps and falling attack sparks. Yellow dialogue states one corrective action. Reduced motion keeps a static indication. Existing authoritative projectiles/effects remain unchanged.
- Reused `DuelTelemetry` for a separate **Dev mode** trial recorder after wand pairing and before entering a duel. The recorder does not need a recognized gesture or successful spell to save a trial. Its small pure state model is separate from React so interruption, numbering, bounded storage and timestamp labels can be tested deterministically. Existing standalone BLE dump tools were inspected; browser capture was selected to retain exactly the production decoder/classifier/speech path without competing for the badge's Bluetooth connection.
- Export now includes firmware/profile and current lesson, microphone, recognition, gesture and fusion state. Suppressed microphone clips explicitly report `recognition-paused` or `overlapping-speech` in developer diagnostics. Tutorial reading screens intentionally do not transcribe; the recording panel runs outside those pauses.

## Verification record

- New default-profile raised-pose matrix: 16/16 synthetic combinations recognized, versus 3/16 before. Existing calibrated motion and recorded-jab regressions passed. This is deterministic software evidence, not measured badge accuracy.
- Focused fusion/controller/speech checks: 58 passed, including no cast/state change on miscast, fresh subsequent success, no fizzle for silent/cancelled input, and explicit suppression diagnostics.
- Trial model: 26 tests passed for sequential capture, missed recognition, partial export, interruption, time/source changes, microphone readiness/generation, speech exclusion and bounds. Review fixes preserve the last active-window packets at interruption without advancing into future trial windows. Spoken trials require a ready microphone and stop if it fails or restarts; movement-only/negative exports exclude speech events and metadata.
- Independent read-only review found three recorder issues (microphone continuity, speech exclusion and interruption tails); all were fixed and the final review found no remaining P0–P3 defects. Physical badge qualification remains explicitly open.
- Full `apps/host/.venv/bin/python tools/qa_game.py`: **109 host tests, 32 tooling tests, 259 web tests, typecheck, production build and 29 browser tests passed** (browser suite 3.2 minutes). The browser suite exercises wand-first entry, real referee two-player combat, all five spells, per-spell cooldowns, synchronized damage/effects, victory/defeat and rematch, solo/tutorial, input interruption and local speech capture. Raw packet fixtures substitute for physical BLE; recognized speech is scripted for combat.
- Added browser scenarios verify Protego, Episkey and attack miscasts, yellow corrections, unchanged HP/shield/cooldowns on both sides, no authoritative projectile, reduced motion and a succeeding next cast. Recorder E2E saves a completed real-time trial and an interrupted next attempt, checks raw bytes/labels/notes/export and a 390 px layout. Inspected desktop/mobile recorder and all three fizzle screenshots; arena previews were posted to Sai. Screenshots: `/tmp/wandduel-{shield,attack,heal}-fizzle-preview.png`, `/tmp/wandduel-gesture-trial-recorder{,-mobile}.png`.
- Sai's final HUD refinement removes Wizard/Duelist captions and places own HP upper-left above the player, rival HP lower-right beneath the opponent. Active shield/disarm/fainted states remain. Production build/typecheck passed again; the existing focused browser scenario passed in 15.7 seconds with desktop/390 px ownership and layout checks after real damage (YOU 100, rival 80). Inspected `/tmp/wandduel-hud-{desktop,mobile}.png`; sprites are unobscured and no horizontal overflow occurs. Earlier full-suite evidence precedes only this presentation change.

## Sai's capture contract

1. Open `http://127.0.0.1:5173` after pulling main and restarting `python3 tools/run_game.py`. Enable **Dev mode** and **Connect badge**. Use the badge first; phone captures remain labelled PHONE.
2. Stay on the room-selection screen. Expand **Record gesture trials**. Select **Protego · raise**. Keep **Say the spell while moving · include raw transcripts** selected for combined voice/motion, then **Enable microphone** if needed and wait for calibration. Uncheck it for a movement-only capture; these recordings are diagnostic, not a gameplay mode. Still and Fidget sets omit speech automatically.
3. Start ten trials. Each lasts ten seconds: **3 s countdown → 3 s movement → 2 s settle → 2 s rest**. Hold your normal grip during countdown; perform one comfortable raise during the movement cue, briefly hold, then follow **Return to your resting grip** and **Rest**. When speech is selected, say the intended spell while moving. Do not exaggerate failed movements just to make the recognizer pass.
4. Add short observations to any missed or unusual trial, then **Export trials JSON**. Export before Reset. Send the file even if recognition failed; raw input is retained independently.
5. Repeat Episkey, Stupefy, Expelliarmus and Incendio. The three attacks share a jab; the two support spells share a held raise. Separate spell sets also expose speech/movement timing differences. Record Still and Fidget negative sets to check that increased tolerance does not create unintended casts.
6. If disconnected, hidden or interrupted, export the partial session. Spoken sessions also stop if the microphone fails or restarts. Do not merge across devices or interrupted input generations. Start a fresh set after recovery. Keep a few later trials/another normal grip untouched for validation after tuning.

No firmware change is required for this iteration. Real badge cadence, grip diversity, noisy-room voices and physical success/false-positive rates remain to be measured from these captures. No microphone audio is recorded or uploaded; the explicit JSON export may include raw local transcription text.
