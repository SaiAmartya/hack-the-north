# Teammate motion recordings

These CSV captures and `apps/web/src/input/fixtures/badge-seven-2026-09-19.json` were preserved from teammate commit `28c2770` during the final-sprint merge. Their seven names describe the experiment that recorded them; they do not enable seven gameplay moves or qualify the current firmware.

The current game has five active spells, selected by speech with a jab for attacks or a held raise for shield/heal. Its existing default gesture profile remains unchanged. The recordings remain available for future measured input work. `tools/wand_motion_log.py` is the accompanying optional raw BLE logger; no device recording or flashing was performed during reconciliation.
